version: '3.7'

services:

  apache:
    build:
      context: .
      dockerfile: apache/Dockerfile
      target: apache

  php:
    build:
      context: .
      dockerfile: php/Dockerfile
      target: php

  php-debug:
    build:
      context: .
      dockerfile: php/Dockerfile
      target: php-debug